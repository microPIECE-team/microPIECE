#!/bin/bash
BUILD_DATE=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
VCS_REF=$(echo ${SOURCE_COMMIT} | cut -b 1-7)

docker build \
       --build-arg BRANCH=$SOURCE_BRANCH \
       --build-arg VCS_REF=${VCS_REF} \
       --build-arg BUILD_DATE=${BUILD_DATE} \
       -t $IMAGE_NAME .
